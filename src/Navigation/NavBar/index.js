import './NavBar.css';
import ModeButton from '../ModeButton';

function NavBar(){
    return (
        <nav>
            <h1>Where in the world?</h1>
            <ModeButton />
        </nav>
    );
}

export default NavBar;
